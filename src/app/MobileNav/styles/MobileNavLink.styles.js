import styled from 'styled-components';
import { Link } from 'gatsby';

export const NavigationLink = styled(Link)`
  color: var(--text-gray);

  &:hover,
  &:active {
    color: var(--text-gray);
  }
`;
